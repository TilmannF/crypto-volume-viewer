/**
 * Large self-contained UI sections (app-shell, directory-browser,
 * extraction-panel, status-bar). Widgets compose features/entities
 * and must not call Tauri commands directly.
 */
export {};

/**
 * Tauri command client and DTO definitions (commands.ts, dto.ts, errors.ts).
 * This is the only place in the frontend allowed to import `invoke`/`listen`
 * from `@tauri-apps/api`.
 */
export {};

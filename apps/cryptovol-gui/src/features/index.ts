/**
 * User actions that provide product value (open-container,
 * browse-directory, extract-file, cancel-extraction, close-session).
 * Features may call shared/api and must not import from other feature
 * slices.
 */
export {};
